repos:
- repo: local
  hooks:
  - id: pylint
    name: pylint
    entry: pylint
    args: [semantic_locomotion]
    pass_filenames: false
    language: system
    types: [python]
    always_run: true
  - id: pytype
    name: pytype
    entry: pytype
    args: [semantic_locomotion, "--config=pytype.cfg"]
    pass_filenames: false
    language: system
    types: [python]
    always_run: true
